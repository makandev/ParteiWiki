# --- Web / API ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
jinja2==3.1.4
python-multipart==0.0.17

# --- Datenbank ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Konfiguration / Validierung ---
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Ingestion / Diff-Tracking ---
httpx==0.28.1
# RSS-Ingestion (späterer Ausbau, im MVP noch nicht genutzt):
# feedparser==6.0.11

# --- Tests ---
pytest==8.3.4

# --- Optionale Backends (nicht für den Offline-Default nötig) ---
# Echtes multilinguales RAG-Embedding (EMBEDDER=sbert):
# sentence-transformers==3.3.1
# Deutsches NER-Modell für den SpacyTagger:
# spacy==3.8.2
#   python -m spacy download de_core_news_sm
